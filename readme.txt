# packages required
#install packages
flask
flask_socketio

# input format num1 + num2;num1 * num2; num1 / num2; -----
#to run the code

python main.py

# The url by default is 'http://127.0.0.1:5000'

# The following line is also seen when the script is executed, 

"Running on http://127.0.0.1:5000/"

# to kill the script select [ctrl+c]

# Please make sure the template folder is in the same path as main.py when executing.

# Sessions are related to browsers. if running on same machine, different browser should be used. (chrome, Edge / chrome - firefox / ...)